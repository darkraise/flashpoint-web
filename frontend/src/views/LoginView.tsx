import { LoginForm } from '@/components/auth/LoginForm';

/**
 * LoginView - Login page using PublicLayout
 * Layout handles centering and background effect
 */
export function LoginView() {
  return <LoginForm />;
}
