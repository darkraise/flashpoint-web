# Game Service Environment Template (Local Development)
# Copy to .env and fill in required values.

# Flashpoint Installation Path
# All other paths (htdocs, games) are automatically derived from this
FLASHPOINT_PATH=D:/Flashpoint

# Logging
LOG_LEVEL=info
NODE_ENV=development

# CGI Support (optional)
# Enable PHP script execution via php-cgi
ENABLE_CGI=false

# CGI settings are loaded from proxySettings.json:
# - phpCgiPath: Path to php-cgi executable (auto-detected from FLASHPOINT_PATH/Legacy/php-cgi.exe)
# - cgiTimeout: Script execution timeout in ms (default: 30000)
# - cgiMaxBodySize: Max request body size in bytes (default: 10MB)
# - cgiMaxResponseSize: Max response size in bytes (default: 50MB)
